
<script>
    window.partytown = {
        debug: true,
        // forward: ['dataLayer.push', '_learnq.push'],
        lib: '/apps/partytown/',
        resolveUrl: (url, location, type) => {
            if (type === 'script') {
                var proxyUrl = new URL('https://my-reverse-proxy.com/');
                proxyUrl.searchParams.append('url', url.href);
                return proxyUrl;
            }
            return url;
        }
    };
</script>

<script type="module">
    import { partytownSnippet } from "{{ 'partytown.js' | asset_url }}";
    
    (function ( d, t, n ) {
        ( n = d.createElement(t) ).innerHTML = partytownSnippet();
        d.head.appendChild( n )
    })( document, 'script' );
</script>