
<script>
    window.partytown = {
        debug: true,
        // forward: ['dataLayer.push', '_learnq.push'],
        lib: '/apps/partytown/',
        {% comment %} resolveUrl: (url, location, type) => {
            'static.klaviyo.com', 'static-tracking.klaviyo.com'
        } {% endcomment %}
    };
</script>

<script type="module">
    import { partytownSnippet } from "{{ 'partytown.js' | asset_url }}";
    
    (function ( d, t, n ) {
        ( n = d.createElement(t) ).innerHTML = partytownSnippet();
        d.head.appendChild( n )
    })( document, 'script' );
</script>